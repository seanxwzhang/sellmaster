<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">SellMaster</a>
    </div>
    <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
            <li class="active"><a href="#">My products</a></li>
            <li><a href="#order">Sales</a></li>
            <li><a href="#webhook">Webhook History</a></li>
            <li><a href="#contact">Contact and Support</a></li>
        </ul>
    </div>
</div>

<div class="row-offcanvas row-offcanvas-left">
    <div id="sidebar" class="sidebar-offcanvas">
        <div class="col-md-12">
            <h3>Hi {{customer.name}}</h3>
            <ul class="nav nav-pills nav-stacked">

                <li {{#if eBayList}} class="active" {{/if}}><a href="/dashboard">eBat List</a></li>
                <li {{#if shopifyList}} class="active" {{/if}}><a href="/dashboard/shopifyList">Shopify List</a></li>
            </ul>
        </div>
    </div>
    <div id="main">
        {{#if shopifyList }}
            <div class="table-responsive">
                {{> productlist1 shopifyList }}
            </div>
        {{/if}}
        {{#if eBayList}}
            <div class="table-responsive">
                {{> productlist2 eBayList }}
            </div>
        {{/if}}
    </div>
</div>
</div>
